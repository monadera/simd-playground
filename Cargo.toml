[package]
name = "simd-playground"
version = "0.1.0"
edition = "2021"

[features]
simd = []

[dependencies]
itertools = "0.12"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "benchmark"
harness = false